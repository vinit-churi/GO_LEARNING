# Answers

## Exercise 1
Create a map with `make(map[string]int)` and set each name to `0`.

## Exercise 2
Use the two-value lookup form so you return both the score and whether the key was present.

## Exercise 3
Loop over the slice and increase each word count by one.
