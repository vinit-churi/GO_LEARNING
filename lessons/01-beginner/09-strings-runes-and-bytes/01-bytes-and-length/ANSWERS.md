# Answers

## Exercise 1
Use `len(text)` to count bytes.

## Exercise 2
Convert the string to `[]rune` and return the first element.

## Exercise 3
Reverse a rune slice in place, then convert it back to `string`.
