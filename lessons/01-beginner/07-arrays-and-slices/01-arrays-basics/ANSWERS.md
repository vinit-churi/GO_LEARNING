# Answers

## Exercise 1
Read each array element by index and add them together.

## Exercise 2
Return `append(tasks, task)` so the new slice includes the extra value.

## Exercise 3
Use `days[5:7]` when the input holds a full seven-day week in order.
