# Answers

## Exercise 1
Build a readable string from the `Name` and `Value` fields.

## Exercise 2
Use `c.Value += amount` inside a pointer receiver.

## Exercise 3
Set the stored value back to zero.
