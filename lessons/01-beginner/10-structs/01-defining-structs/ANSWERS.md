# Answers

## Exercise 1
Return a `Book` literal with the provided title and page count.

## Exercise 2
Loop over the slice and add the `Pages` field from each book.

## Exercise 3
Read the nested `Customer` and `Branch` data and format them into one string.
