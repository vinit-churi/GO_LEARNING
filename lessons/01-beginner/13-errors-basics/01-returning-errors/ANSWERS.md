# Answers

## Exercise 1
Trim the input, reject empty text, and use `strconv.Atoi` to parse the number.

## Exercise 2
Return an error when `b` is zero before dividing.

## Exercise 3
Reject blank names or non-positive quantities, then build the success string.
