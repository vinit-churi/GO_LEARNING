# Answers

## Exercise 1

Use an accumulator and add the current day number on each iteration.

## Exercise 2

Loop backward from `start` to `1` and append each number into a slice.

## Exercise 3

Iterate from `0` to `limit` and only add values where `i % 2 == 0`.
